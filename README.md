# ☕ Nada Coffee Frontend Project

나다커피(Nada Coffee)의 신선한 원두와 음료를 만나볼 수 있는 프리미엄 커머스 플랫폼입니다.

---

## 🚨 백엔드 개발자 요청 사항 (Urgent Requests)

원활한 기능 완성을 위해 다음 API 및 DB 설정 변경을 요청드립니다.

### 1. 포인트 시스템 (Point System)
- **신규 가입 혜택**: 회원가입(`POST /api/auth/signup`) 성공 시, 해당 유저에게 **기본 5,000포인트**를 즉시 지급하도록 로직 추가 부탁드립니다.
- **구매확정 적립**: 주문 상태가 `PURCHASE_COMPLETED`로 변경될 때, **실제 결제 금액의 1%**를 포인트로 자동 적립하는 트리거 또는 로직 구현이 필요합니다.
- **포인트 적립 API**: 프론트엔드에서 명시적으로 포인트를 적립할 수 있는 `POST /api/points` API 확인 부탁드립니다.

### 2. 주문 상태 관리 (Order Status)
- **상태 변경 API**: `PATCH /api/admin/orders/{id}` API가 `status` 필드 업데이트를 정상적으로 지원하는지 확인 부탁드립니다.

---

## 📅 작업 현황 (2026-02-06)

### ✅ 오늘 완료한 작업 (Today's Achievements)

#### 1. 포인트 시스템 고도화 💰
- **구매확정 적립 로직**: 마이페이지에서 구매확정 시 결제 금액의 **1%**를 계산하여 사용자에게 알림을 제공하고 포인트 데이터를 즉시 갱신하는 로직 구현.
- **포인트 페이지네이션**: 포인트 이용 내역이 많을 경우를 대비해 10개 단위 페이지네이션 UI 및 API 연동 완료.
- **실시간 잔액 동기화**: 'My 포인트' 메뉴 진입 시 캐시를 무시하고 항상 최신 잔액을 불러오도록 개선.

#### 2. 관심사 분리 및 리팩토링 (Refactoring) 🛠️
- **MyPage 분리**: 거대했던 `MyPage.tsx`를 `useMyPage` 커텀 훅, `MyPageSidebar`, `MyPageContent` 컴포넌트로 분리하여 유지보수성 향상.
- **공통 컴포넌트 구축**: `Button`, `Badge`, `Pagination`, `LoadingSpinner` 등 재사용 가능한 컴포넌트들을 `src/components/common`에 구축.
- **twMerge 최적화**: 전체 프로젝트의 클래스명 관리 방식을 `twMerge([...])` 배열 형태로 변경하여 가독성 및 스타일 안전성 확보.

#### 3. 관리자 기능 개선 👑
- **주문 상태 관리**: ACTIONS 컬럼을 `select` 박스로 변경하여 모든 주문 상태를 자유롭게 변경할 수 있도록 개선.
- **GNB 바로가기**: 관리자 페이지 상단에 메인 사이트의 주요 메뉴로 즉시 이동할 수 있는 GNB 링크 추가.

---

## 🛠 Tech Stack
- **Frontend**: React 19, TypeScript, Vite
- **Styling**: Tailwind CSS v4, Framer Motion, tailwind-merge
- **State Management**: Zustand, TanStack Query v5
- **Integration**: Toss Payments, Daum Postcode API
